import '../styles/globals.css'

/* ONDE FICAM OS COMPONENTES QUE ESTÃO EM TODAS AS PAGUINAS  */

function MyApp({ Component, pageProps }) {
  return <Component {...pageProps} />
}

export default MyApp
